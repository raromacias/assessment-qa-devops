const {shuffleArray} = require('./utils')

describe('shuffleArray should', () => {
    // CODE HERE
    // test('matches if array', () => {
    //     expect().toEqual(expect.arrayContaining(shuffleArray))
    
    //   })
})